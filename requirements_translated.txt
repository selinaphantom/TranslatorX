# TranslateX 相依套件
# 执行：pip install -r requests.txt

# 必要（标准库已内建，无需安装）
# tkinter     — GUI（Python 内建）
# csv         — CSV 处理（Python 内建）
# json        — JSON 处理（Python 内建）
# urllib      — HTTP 请求（Python 内建）
# threading   — 多执行绪（Python 内建）

# 选用（根据功能需求）
openpyxl>=3.1.0      # Excel (.xlsx) 读写支援
pandas>=2.0.0        # 进阶资料分析（可选）